{
  "name": "redirect",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node  src/index.js",
    "dev": "nodemon  src/index.js",
    "build": "npm install && npm install --only=dev && npm install --only=production",
    "pm2:init": "pm2 start pm2.config.json && pm2 save && (pm2 startup || echo \"pm2 startup not supported on this OS, you can ignore this message\")"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ag-grid-community": "^31.0.0",
    "ag-grid-enterprise": "^31.0.0",
    "dayjs": "^1.11.10",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mysql2": "^3.6.5",
    "pug": "^3.0.2"
  }
}
